# Track Your Expenses

The "Track Your Expenses" project, built with React and Vite, allows users to manage expenses by adding a title, selecting a category, and entering an amount. It includes form validation, inline editing, sorting by title, amount, or category, and filtering via a custom hook. A custom context menu enhances interactivity, and all data is saved in localStorage for persistence across sessions.
